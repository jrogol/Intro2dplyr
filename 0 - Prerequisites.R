# Install and/or update the tidyverse package
install.packages("tidyverse")

# Load the tidyverse package
library(tidyverse)

# Check for (and install) additional updates
tidyverse_update()

# Install the nycflights13 package. It's a series of 5 interrelated datasets 
# depicting flights departing the three New York City Airports (JFK, LaGuardia, 
# and Newark) in 2013. It's great for exploring the tidyverse, and worth having
# even if we don't get to it!
install.packages("nycflights13")

# The 'zoo' package will be used for demonstrative purposes, as well.
install.packages("zoo")
